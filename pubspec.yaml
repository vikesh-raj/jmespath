name: jmespath
description: jmespath is a dart implementation of JMESPath, which is a query language for JSON.
version: 2.0.0
homepage: https://github.com/vikesh-raj/jmespath

environment:
  sdk: '>=2.12.0 <3.0.0'

dependencies:
  collection: ^1.15.0
  charcode: ^1.3.1

dev_dependencies:
  pedantic: ^1.11.1
  test: ^1.17.12
  flutter_test:
    sdk: flutter
  lints: ^1.0.1

